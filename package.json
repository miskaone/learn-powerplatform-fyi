{
  "name": "learn-powerplatform-fyi",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun install --frozen-lockfile && bun run --cwd apps/web build",
    "test": "bun test",
    "validate:content": "bun scripts/validate-content.ts"
  }
}
